spring:
  application:
    name: shorten-url-be
  datasource:
    url: jdbc:mysql://quality:3306/shorten-url?allowPublicKeyRetrieval=true&useSSL=false
    username: quality
    password: quality
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

snowflake:
  data-center-id: 1
  worker-id: 1